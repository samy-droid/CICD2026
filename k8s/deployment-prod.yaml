apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-app-prod
  namespace: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-app
      env: prod
  template:
    metadata:
      labels:
        app: hello-app
        env: prod
    spec:
      containers:
        - name: hello-app
          image: DOCKER_HUB_USERNAME/hello-app:prod
          ports:
            - containerPort: 3000
          env:
            - name: APP_ENV
              value: prod
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: hello-app-prod
  namespace: prod
spec:
  selector:
    app: hello-app
    env: prod
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
